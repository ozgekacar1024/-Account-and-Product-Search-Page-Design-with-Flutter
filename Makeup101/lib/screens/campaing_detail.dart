import 'package:flutter/material.dart';

class CampaingDetailPage extends StatelessWidget{
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("CampaingDetailPage"),
      ),
      body: Center(
        child: Text("CampaingDetailPage"),
      ),
    );
  }
}